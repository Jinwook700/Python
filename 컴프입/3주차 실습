[002]두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.
Input
첫째 줄에 A가, 둘째 줄에 B가 주어진다. (A, B는 양의 정수)
Output
첫째 줄에 A+B를 출력한다.

(정답코드)
A = int(input())
B = int(input())
print(A+B)


[003]두 정수 A와 B를 입력받은 다음, A+B를 출력하는 프로그램을 작성하시오.
Input
첫째 줄에 A와 B가 주어진다. (A, B는 양의 정수)
Output
첫째 줄에 A+B를 출력한다.

(정답코드)
A, B = input().split()
A = int(A)
B = int(B)
print(A+B)

A, B = map(int, input().split()) //map 활용
print(A+B)


[004]두 정수 A와 B를 입력받은 다음 아래 코드에 본인의 코드를 추가하여 
A값과 B값을 바꾸어 출력하는 프로그램을 작성하시오.

A, B = map(int,input().split())
#여기에 코드를 추가하세요
B = A
A =#해당 코드를 완성하세요

print(A, B)

Input
첫째 줄에 A와 B가 주어진다. (A, B는 양의 정수)
Output
서로 바꾼 A와 B값을 출력한다.

(정답코드)
A, B = map(int,input().split())
temp = B
B = A
A = temp
print(A, B)


[005]준하는 사이트에 회원가입을 하다가 joonas라는 아이디가 이미 존재하는 것을 보고 놀랐다. 
준하는 놀람을 ??!로 표현한다. 준하가 가입하려고 하는 사이트에 이미 존재하는 아이디가 주어졌을 때, 
놀람을 표현하는 프로그램을 작성하시오.
Input
첫째 줄에 준하가 가입하려고 하는 사이트에 이미 존재하는 아이디가 주어진다. 
아이디는 알파벳 소문자로만 이루어져 있으며, 길이는 50자를 넘지 않는다.
Output
첫째 줄에 준하의 놀람을 출력한다. 놀람은 아이디 뒤에 ??!를 붙여서 나타낸다.

(정답코드)
Name = str(input())
print("%s??!"%Name) //formating


[006]영어 대소문자와 공백으로 이루어진 문자열이 주어진다. 
이 문자열에는 몇 개의 단어가 있을까? 이를 구하는 프로그램을 작성하시오. 
단, 한 단어가 여러 번 등장하면 등장한 횟수만큼 모두 세어야 한다.
Input
첫 줄에 영어 대소문자와 공백으로 이루어진 문자열이 주어진다. 
이 문자열의 길이는 1,000,000을 넘지 않는다. 단어는 공백 한 개로 구분되며, 공백이 연속해서 나오는 경우는 없다. 
또한 문자열은 공백으로 시작하거나 끝날 수 있다.
Output
첫째 줄에 단어의 개수를 출력한다.

(정답코드)
string = input()
words = string.split(' ')
count = len(words)

if words[-1] == '':
    count -= 1
if words[0] == '':
    count -= 1
print(count)


[007] 세 개의 자연수 A, B, C가 주어질 때 A × B × C를 계산한 결과에 
0부터 9까지 각각의 숫자가 몇 번씩 쓰였는지를 구하는 프로그램을 작성하시오.

예를 들어 A = 150, B = 266, C = 427 이라면 A × B × C = 150 × 266 × 427 = 17037300 이 되고, 
계산한 결과 17037300 에는 0이 3번, 1이 1번, 3이 2번, 7이 2번 쓰였다.

(정답코드)
A = int(input())
B = int(input())
C = int(input())
Result = A*B*C
for i in range(0,10):
    print(str(Result).count(str(i)))
